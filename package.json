{
  "name": "redirect-me",
  "version": "1.0.0",
  "description": "A simple extension to redirect URLs",
  "scripts": {
    "build": "node scripts/build.js",
    "minifycss": "npx @tailwindcss/cli -i ./src/input.css -o ./src/tailwind.css --minify",
    "watch": "npx @tailwindcss/cli -i ./src/input.css -o ./src/tailwind.css --watch"
  },
  "author": "Adriano Resende",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "archiver": "^7.0.1",
    "fs-extra": "^11.2.0",
    "html-minifier-terser": "^7.2.0",
    "tailwindcss": "^4.1.11",
    "terser": "^5.31.3"
  }
}
